#
# Windows Workflow
#

# https://github.com/filipdutescu/modern-cpp-template/blob/master/.github/workflows

name: Windows

on:
  push:
    branches: [ release, next ]
  pull_request:
    branches: [ release, next ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        compiler:
          - msvc
        compiler_c_short_name:
          - msvc
        compiler_cxx_short_name:
          - msvc
        build_type:
          - Release
        install_location:
          - .local
        developer_mode:
          - OFF

    runs-on: windows-latest
    if: "!contains(github.event.head_commit.message, '[skip ci]') && !contains(github.event.head_commit.message, '[skip ci windows]')"

    steps:
      - uses: actions/checkout@v3

      - name: Cache
        uses: actions/cache@v3
        id: cache
        with:
          path: |
            ${{github.workspace}}/${{matrix.install_location}}
            ${{github.workspace}}/build/out
            ${{github.workspace}}/cmake
          key: ${{runner.os}}-dependencies

      - name: Setup Cpp
        uses: aminya/setup-cpp@v1
        with:
          compiler: ${{matrix.compiler}}
          vcvarsall: ${{contains(matrix.os, 'windows')}}
          cmake: true
          ccache: false
          ninja: false
          conan: false
          vcpkg: false
          gcovr: false
          cppcheck: false
          clangtidy: false
          opencppcoverage: false

      - name: Build
        run: |
          bash -c 'build/build this --no-build-debug'

      - name: Test Runnable
        run: build/out/this/release/Release/wtr.watcher.exe . -s 1
